name: KB1036083

type: any

index: logstash*

filter:
- query:
    query_string:
      query: "msg_text: \"Upgrade is required since hwVersion in config file is 3\""

realert:
  minutes: 0

alert:
  - command
command: ["ls","/dev/null"]

match_enhancements:
- "elastalert_modules.rc_enhancements.InvCorrEnhancement"
